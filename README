Single page system that triggers the actions displayed when the page is submitted asynchronously.

The actions have some risk of failure, the result of the action is fed back to the page using SignalR to call client side javascript methods.